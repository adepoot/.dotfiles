# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango:monospace 8, Font Awesome 5 Brands 8
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run -i
#bindsym $mod+d exec "dmenu_run -i -nf '#BBBBBB' -nb '#222222' -sb '#669C66' -sf '#EEEEEE' -fn 'monospace-10'"
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
#bindsym $mod+h split h

# split in vertical orientation
#bindsym $mod+v split v
bindsym $mod+v split toggle

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Set workspace names
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
#bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym semicolon resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        #status_command i3status
        status_command i3blocks -c ~/.config/i3blocks/conf
        font pango: DejaVu Sans Mono, Font Awesome 5 Brands, Font Awesome 5 Free Regular, Font Awesome 5 Free Solid 8
        #font pango: DejaVu Sans Mono 8

        #colors {
        #        background #000000
        #        statusline #FFFFFF
        #        separator  #666666

        #        focused_workspace  #004D00 #004D00 #FFFFFF
        #        active_workspace   #333333 #222222 #FFFFFF
        #        inactive_workspace #333333 #222222 #888888
        #        urgent_workspace   #ff9900 #ff9900 #000000
        #        binding_mode       #2F343A #ff9900 #000000
        #}
}

# Custom bindsyms
#bindsym $mod+greater move workspace to output right # Move an entire workspace to the right
bindsym $mod+greater exec --no-startup-id i3-msg move workspace to output right
#bindsym $mod+less move workspace to output left # Move an entire workspace to the left
bindsym $mod+less exec --no-startup-id i3-msg move workspace to output left
bindsym $mod+question exec --no-startup-id i3-msg move workspace to output up
bindsym $mod+n exec --no-startup-id nautilus  # Open filesystem

# Printscreen
bindsym Print exec gnome-screenshot -i

# Calculator
bindsym XF86Calculator exec --no-startup-id gnome-calculator

# Pulse Audio controls
bindsym $mod+m exec --no-startup-id pavucontrol
bindsym XF86AudioRaiseVolume exec --no-startup-id volume-increase && pkill -RTMIN+10 i3blocks
bindsym XF86AudioLowerVolume exec --no-startup-id volume-decrease && pkill -RTMIN+10 i3blocks
bindsym XF86AudioMute exec --no-startup-id volume-toggle-mute && pkill -RTMIN+10 i3blocks

# Screen brightness controls
#bindsym XF86MonBrightnessUp exec xbacklight -inc 20 # increase screen brightness
#bindsym XF86MonBrightnessDown exec xbacklight -dec 20 # decrease screen brightness
bindsym XF86MonBrightnessUp exec brightnessctl set 20%+ # increase screen brightness
bindsym XF86MonBrightnessDown exec brightnessctl set 20%- # decrease screen brightness

# Media player controls
bindsym XF86AudioPlay exec playerctl play-pause
#bindsym XF86AudioPause exec playerctl play-pause
bindsym XF86AudioStop exec playerctl stop
bindsym XF86AudioNext exec playerctl next
bindsym XF86AudioPrev exec playerctl previous

# Background
#set $wallpaper /usr/share/backgrounds/hardy_wallpaper_uhd.png
#set $wallpaper ~/Pictures/wallpapers/peru_squirel-monkey.jpg
#exec_always feh --bg-fill $wallpaper
#exec_always feh --bg-center $wallpaper
#exec --no-startup-id feh --randomize --bg-fill ~/Pictures/wallpapers/*
#exec_always feh --bg-center ~/Pictures/wallpapers/GemOne/HD_wallpaper_3840X2160px3.jpg
exec_always feh --bg-fill ~/Pictures/wallpapers/GemOne/HD_wallpaper_3840X2160px3.jpg
#exec_always --no-startup-id variety

# Auto screen config
bindsym $mod+Shift+s exec --no-startup-id screenlayout-auto-detect

# Lock screen
set $lockpaper ~/Pictures/wallpapers/GemOne/HD_wallpaper_3840X2160px2.jpg
#exec --not-startup-id betterlockscreen -u $lockpaper -r 1920x1080 # cache the image
exec --no-startup-id betterlockscreen -u $lockpaper # cache the image
#set $lockpaper ~/Pictures/wallpapers/GEM_One_inverted.png
#exec --not-startup-id betterlockscreen -u $lockpaper # cache the image
#set $i3lockscreen betterlockscreen -l blur -t "Password: 1234"
set $i3lockscreen betterlockscreen -l
bindsym $mod+Ctrl+Shift+l exec --no-startup-id $i3lockscreen

# Startup apps
exec --no-startup-id xrandr --dpi 96 # Changing resolution
exec --no-startup-id screenlayout-auto-detect
exec --no-startup-id numlockx on        # Enable numlock by default
exec --no-startup-id start-pulseaudio-x11   # Start PA in background to prevent delay on first use
#exec --no-startup-id xautolock -time 5 -locker $i3lockscreen &
exec --no-startup-id solaar --window=hide # Solaar configures Logitech products

# Copyq
exec --no-startup-id copyq
bindsym $mod+c exec --no-startup-id copyq toggle

# Trays
exec --no-startup-id pasystray                  # The volume applet
exec --no-startup-id caffeine-indicator         # The caffeine applet
exec --no-startup-id blueman-applet             # The bluetooth applet
exec --no-startup-id nm-applet                  # The network manager applet
exec --no-startup-id xfce4-power-manager        # The power manager applet

# If you hit the same workspace again, you go to the previous workspace
workspace_auto_back_and_forth yes

# Lock
bindsym XF86ScreenSaver exec --no-startup-id $i3lockscreen

set $mode_screenlayout Choose screen layout: (l) laptop, (h) home, (w) work
mode "$mode_screenlayout" {
    bindsym l exec --no-startup-id /home/adepoot/.screenlayout/screenlayout-one.sh, mode "default"
    bindsym h mode "$mode_screenlayout_home"
    bindsym w mode "$mode_screenlayout_work"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Ctrl+s mode "$mode_screenlayout"
set $mode_screenlayout_home Choose home screen layout: (u) laptop up, (d) laptop down
mode "$mode_screenlayout_home" {
    bindsym u exec --no-startup-id /home/adepoot/.screenlayout/screenlayout-three-home-docking.sh, mode "default"
    bindsym d exec --no-startup-id /home/adepoot/.screenlayout/screenlayout-three-home-laptop.sh, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
set $mode_screenlayout_work Choose work screen layout: (s) laptop to the side, (d) laptop down
mode "$mode_screenlayout_work" {
    bindsym s exec --no-startup-id /home/adepoot/.screenlayout/screenlayout-three-work.sh, mode "default"
    bindsym d exec --no-startup-id /home/adepoot/.screenlayout/screenlayout-three-work-laptop.sh, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# shutdown / restart / suspend...
set $mode_system System (l) lock, (e) logout, (s) suspend, (h) hibernate, (r) reboot, (Ctrl+s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id $i3lockscreen, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym s exec --no-startup-id $i3lockscreen && systemctl suspend, mode "default"
    bindsym h exec --no-startup-id $i3lockscreen && systemctl hibernate, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym Ctrl+s exec --no-startup-id systemctl poweroff -i, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Escape mode "$mode_system"

# GCP
set $mode_gcp GCP (a) Artifact Registry, (b) BigQuery, (c) Cloudbuild, (d) Cloud DNS, (e) Data Studio, (k) Kubernetes, (l) Logging, (m) Monitoring, (p) Pubsub, (s) Storage
mode "$mode_gcp" {
        bindsym a exec --no-startup-id google-chrome https://console.cloud.google.com/artifacts?project=happy-fleet-host, mode "default"
        bindsym p mode "$mode_pubsub"
        bindsym s mode "$mode_storage"
        bindsym c exec --no-startup-id google-chrome https://console.cloud.google.com/cloud-build/builds?project=happy-fleet-host, mode "default"
        bindsym k mode "$mode_kubernetes"
        bindsym l mode "$mode_logging"
        bindsym m mode "$mode_monitoring"
        bindsym d mode "$mode_dns"
        bindsym b mode "$mode_bigquery"
        bindsym e exec --no-startup-id google-chrome https://datastudio.google.com/u/0/reporting/43ba8534-c336-4133-8577-f576524d548a/page/p_rc1j7i4doc, mode "default"

        # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+g mode "$mode_gcp"

# Pubsub
set $mode_pubsub GCP Pubsub (d) DEV, (q) QA, (p) PROD
mode "$mode_pubsub" {
        bindsym d exec --no-startup-id google-chrome https://console.cloud.google.com/cloudpubsub/topic/list?project=happy-fleet-service-dev, mode "default"
        bindsym q exec --no-startup-id google-chrome https://console.cloud.google.com/cloudpubsub/topic/list?project=happy-fleet-service-qa, mode "default"
        bindsym p exec --no-startup-id google-chrome https://console.cloud.google.com/cloudpubsub/topic/list?project=happy-fleet-service-prod, mode "default"

        # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Storage
set $mode_storage GCP Storage (d) DEV, (q) QA, (p) PROD, (h) HOST
mode "$mode_storage" {
        bindsym d exec --no-startup-id google-chrome https://console.cloud.google.com/storage/browser?project=happy-fleet-service-dev, mode "default"
        bindsym q exec --no-startup-id google-chrome https://console.cloud.google.com/storage/browser?project=happy-fleet-service-qa, mode "default"
        bindsym p exec --no-startup-id google-chrome https://console.cloud.google.com/storage/browser?project=happy-fleet-service-prod, mode "default"
    bindsym h exec --no-startup-id google-chrome https://console.cloud.google.com/storage/browser?project=happy-fleet-host, mode "default"

        # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Kubernetes
set $mode_kubernetes GCP Kubernetes (d) DEV, (q) QA, (p) PROD
mode "$mode_kubernetes" {
        bindsym d exec --no-startup-id google-chrome https://console.cloud.google.com/kubernetes/workload?project=happy-fleet-service-dev, mode "default"
        bindsym q exec --no-startup-id google-chrome https://console.cloud.google.com/kubernetes/workload?project=happy-fleet-service-qa, mode "default"
        bindsym p exec --no-startup-id google-chrome https://console.cloud.google.com/kubernetes/workload?project=happy-fleet-service-prod, mode "default"

        # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Logging
set $mode_logging GCP Logging (d) DEV, (q) QA, (p) PROD
mode "$mode_logging" {
        bindsym d exec --no-startup-id google-chrome https://console.cloud.google.com/logs/query?project=happy-fleet-service-dev&query=%0A, mode "default"
        bindsym q exec --no-startup-id google-chrome https://console.cloud.google.com/logs/query?project=happy-fleet-service-qa&query=%0A, mode "default"
        bindsym p exec --no-startup-id google-chrome https://console.cloud.google.com/logs/query?project=happy-fleet-service-prod&query=%0A, mode "default"

        # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Monitoring
set $mode_monitoring GCP Monitoring (d) DEV, (q) QA, (p) PROD
mode "$mode_monitoring" {
        bindsym d exec --no-startup-id google-chrome https://console.cloud.google.com/monitoring/dashboards?project=happy-fleet-service-dev, mode "default"
        bindsym q exec --no-startup-id google-chrome https://console.cloud.google.com/monitoring/dashboards?project=happy-fleet-service-qa, mode "default"
        bindsym p exec --no-startup-id google-chrome https://console.cloud.google.com/monitoring/dashboards?project=happy-fleet-service-prod, mode "default"

        # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Cloud DNS
set $mode_dns GCP Cloud DNS (d) DEV, (p) PROD, (h) HOST
mode "$mode_dns" {
        bindsym d exec --no-startup-id google-chrome https://console.cloud.google.com/net-services/dns/zones?project=happy-fleet-service-dev, mode "default"
        bindsym p exec --no-startup-id google-chrome https://console.cloud.google.com/net-services/dns/zones?project=happy-fleet-service-prod, mode "default"
        bindsym h exec --no-startup-id google-chrome https://console.cloud.google.com/net-services/dns/zones?project=happy-fleet-host, mode "default"

        # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# BigQuery
set $mode_bigquery GCP BigQuery (d) DEV, (q) QA, (p) PROD
mode "$mode_bigquery" {
        bindsym d exec --no-startup-id google-chrome https://console.cloud.google.com/bigquery?project=happy-fleet-service-dev, mode "default"
        bindsym q exec --no-startup-id google-chrome https://console.cloud.google.com/bigquery?project=happy-fleet-service-qa, mode "default"
        bindsym p exec --no-startup-id google-chrome https://console.cloud.google.com/bigquery?project=happy-fleet-service-prod, mode "default"

        # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# Set default workspace for classes
assign [class="Spotify"] $ws10
assign [class="jetbrains-idea"] $ws3
assign [class="jetbrains-clion"] $ws3
assign [class="Code"] $ws3
assign [class="jetbrains-pycharm-ce"] $ws3
assign [class="jetbrains-pycharm"] $ws3
assign [class="Slack"] $ws5
assign [class="VirtualBox Manager"] $ws6
assign [class="VirtualBox Machine"] $ws6
assign [class="google-chrome"] $ws9

# Windows that should float automatically
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [class="Arandr"] floating enable
for_window [class="Pavucontrol"] floating enable
for_window [class="Nm-connection-editor"] floating enable
for_window [class="Gnome-calculator"] floating enable
for_window [class="Gnome-todo"] floating enable
for_window [class="copyq"] floating enable

# class                 border  bground text    indicator child_border
#client.focused          #004D00 #004D00 #FFFFFF #00B500   #004D00
#client.focused_inactive #333333 #5F676A #FFFFFF #484E50   #5F676A
#client.unfocused        #333333 #222222 #888888 #292D2E   #222222
#client.urgent           #ff9900 #ff9900 #000000 #ff9900   #ff9900
#client.placeholder      #000000 #0C0C0C #FFFFFF #000000   #0C0C0C

#client.background       #FFFFFF